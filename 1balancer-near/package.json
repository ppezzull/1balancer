{
  "name": "@1balancer/near",
  "version": "1.0.0",
  "description": "NEAR Protocol integration for 1Balancer",
  "private": true,
  "scripts": {
    "dev": "echo 'NEAR functionality is integrated into the orchestrator service. Run: cd ../packages/orchestrator && yarn dev'",
    "build": "echo 'NEAR functionality is built with orchestrator. Run: cd ../packages/orchestrator && yarn build'",
    "test": "echo 'NEAR tests are in orchestrator. Run: cd ../packages/orchestrator && yarn test'",
    "deploy": "./scripts/deploy-local.sh",
    "deploy:testnet": "echo 'Testnet deployment not configured yet'",
    "deploy:mainnet": "echo 'Mainnet deployment not configured yet'"
  },
  "keywords": [
    "near",
    "cross-chain",
    "atomic-swaps",
    "htlc"
  ],
  "author": "1Balancer Team",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}